/*
 * Created on 2023-01-03 ( 08:34:10 )
 * Generated by Telosys ( http://www.telosys.org/ ) version 4.0.0
 */
package org.demo.entities;

import static junit.framework.TestCase.assertNotNull;
import static junit.framework.TestCase.assertNull;

import org.demo.jpa.tools.JpaTest;
import org.junit.Test;

public class ProwadzacyJpaTest extends JpaTest {
	
    @Override
    public void initData() {
		// DatabaseData.init(em) has been called before
		// Initialize here other data required for the current test
    }
	
    private Prowadzacy createEntity() {
    	Prowadzacy entity = new Prowadzacy();
		entity.setIdProwadz( 200 ) ;
		entity.setIdPrzedmiotu( Integer.valueOf(200) ) ;
		entity.setImie( "BBBBBBBBBBBBBBBBBBBBBBBBBBBBBB" ) ;
		entity.setNazwisko( "BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB" ) ;
		entity.setEmailProwadzacego( "BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB" ) ;
		entity.setNrTelefonu( Integer.valueOf(200) ) ;
		entity.setDataZatrudnienia( java.time.LocalDate.parse("2002-06-22") ) ;
    	return entity;
    }

    private Object getEntityKey(Prowadzacy entity) {
    	return entity.getIdProwadz();
    }

    @Test
    public void testPersistFindRemove() {
    	
    	Prowadzacy entity = createEntity();
    	Object key = getEntityKey(entity);

    	// if entity doesn't exist yet
    	if ( find(Prowadzacy.class, key) == null ) {

	    	// Create with 'persist'
    		persistAndRefresh(entity);
    	
			// Find
			Prowadzacy entity2 = find(Prowadzacy.class, key);
			assertNotNull(entity2);
        
			// Remove
			remove(entity2);
			assertNull(find(Prowadzacy.class, key));
		}
    }
    
    @Test
    public void testMergeFindRemove() {
    	
    	Prowadzacy entity = createEntity();
    	Object key = getEntityKey(entity);

    	// Create or update with 'merge'
    	mergeAndRefresh(entity);
    	
    	// Find
    	Prowadzacy entity2 = find(Prowadzacy.class, key);
        assertNotNull(entity2);
        
    	// Remove
    	remove(entity2);
        assertNull(find(Prowadzacy.class, key));
    }

}
